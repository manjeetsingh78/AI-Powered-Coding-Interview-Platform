replicaCount: 2

backend:
  image: REPLACE_WITH_BACKEND_IMAGE
  port: 8000
  resources:
    requests:
      cpu: 250m
      memory: 256Mi
    limits:
      cpu: 1000m
      memory: 1024Mi
  serviceAccountRoleArn: ""

frontend:
  image: REPLACE_WITH_FRONTEND_IMAGE
  port: 80

service:
  type: ClusterIP
