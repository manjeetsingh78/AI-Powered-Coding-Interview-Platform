{{- if not .Values.backend.skip }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "interview-platform.fullname" . }}-grafana-dashboards
  labels:
    grafana_dashboard: "1"
data:
  backend-dashboard.json: |
{{ (.Files.Get "deploy/observability/grafana/dashboards/backend-dashboard.json") | indent 4 }}
{{- end }}
