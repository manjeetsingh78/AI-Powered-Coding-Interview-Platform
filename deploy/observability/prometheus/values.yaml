# Sample overrides for kube-prometheus-stack
prometheus:
  prometheusSpec:
    retention: 15d
    evaluationInterval: 1m

# Add additional scrape configs via extraScrapeConfigsSecret or ServiceMonitors
